══════════════════════════════════════════════════════════════════
游戏开发班  ·  检查点文档 v1.0
下次上课：下下周一（清明节后）
五一目标：各组第一版游戏完成
发布位置：GitHub 各组分支
══════════════════════════════════════════════════════════════════

⚠️ 说在前面
下周一清明节放假，好好玩，别摸鱼写代码。
回来之后，各组下下周一上课时必须现场跑通一关。
PPT 展示不算，Unity 里能玩才算。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROUP A  许维家（单人·街头武侠动作）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前进度（本次课）：
✅ 基础碰撞盒 Capsule/Box collider 已绑定
✅ 命中判定（命中变红）已实现
✅ Mixamo 动作导入 Unity 跑通
✅ AI 辅助编写代码（用 Gemini）

▌ 下下周一检查点（必须完成）

  CP-A1  硬直系统（Hitstun）
  命中敌人后，敌人进入约 0.3–0.5 秒的"硬直"状态，
  期间不能攻击、不能防御。
  问 AI：Unity Hitstun system implementation

  CP-A2  不可防御状态（Unblockable window）
  特定攻击（如重击）命中后，触发更长硬直，
  无法被格挡。
  这两个状态是打击感的核心，
  没有它们，战斗永远是"乒乒乓乓没有重量"。

  CP-A3  受击盒随姿态切换
  站立/蹲下/空中 三种姿态，
  对应不同的 hitbox 尺寸和位置。
  最低要求：站立和蹲下两种状态切换。
  问 AI：dynamic hitbox switching based on animation state Unity

  CP-A4  批量碰撞盒（加分项，本周争取）
  让 AI（Gemini）帮你生成批量绑碰撞盒的脚本，
  不要每个部位手动绑——这是可以自动化的几何数学问题。
  解决了，其他组全可以借用你的方法。

▌ GitHub 提交要求
  分支名：group-a-xu
  提交内容：能跑的 Unity 工程 + 一个能打的测试关卡

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROUP B  5人组（捉妖镇·聊斋侦探）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前进度（本次课）：
✅ 美术素材明显推进（场景/人物/道具/地图）
✅ 整体美术风格统一，多人分工有整体感
✅ 引导关卡已有初步实现（遮罩 modal window 模式）
✅ 死亡/结算界面已有设计稿
⚠️ 展示电脑性能问题，完整效果未能全展示

▌ 下下周一检查点（必须完成）

  CP-B1  第一只画皮妖能被抓到（完整一局可通关）
  不要求所有线索都完善，
  但玩家能从"进入村庄"走到"指认画皮妖"再到"胜利/失败结算"，
  全流程不崩溃。

  CP-B2  Trigger 系统基础搭建（本课重点要求）
  每一个关键对话/道具/线索的触发，
  必须有至少 2 个前置条件（时间 or 空间 or 道具）。
  最低实现形式：用 if-else 写死，
  下一轮再重构成可配置系统。
  示范：老板娘只在"夜晚"AND"玩家持有某道具"时才说真话。

  CP-B3  概率 Trigger（至少 1 处）
  至少一个关键线索的触发，不是 100% 必然，
  是 30%~50% 的概率触发，
  让玩家需要多次互动才能拿到。
  防止玩家两次就解谜结束。

  CP-B4  引导系统改进
  引导提示框不要用 modal（强制中断）模式，
  改成悬浮提示，玩家可随时关闭。
  第一次进入新区域才触发，不重复弹。

  CP-B5  美术整合（加分项）
  怪物在场景中是否清晰可辨？
  考虑：战斗时场景虚化（depth of field），
  让玩家注意力集中在战斗目标上。

▌ 下下周前完成（清明假期任务）
  5 人分工角色扮演：
  1 人扮演画皮妖（设计它的行为逻辑：害怕什么、习惯什么、
    不得不做什么），
  其余 4 人各扮演 1 个 NPC，
  单独把自己的角色的行为习惯、台词方式写出来，
  然后合在一起看在哪些时空条件下能碰撞出有效线索。

▌ GitHub 提交要求
  分支名：group-b-liaozhai
  提交内容：Unity 工程 + 第一局可完整进行的场景

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROUP C  方兰组（海洋馆·微恐怖探索）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前进度（本次课）：
✅ 过场动画艺术效果非常不错（谭老师原话：满分）
✅ 整体恐怖氛围感在线，有创意细节（水迹脚印、悬挂海豚）
✅ 多边形低面风格整体一致
⚠️ 玩法交互尚不明确——玩家不知道怎么赢、怎么输
⚠️ 线索与空间结构尚未绑定（事件图未与地图联动）

▌ 下下周一检查点（必须完成）

  CP-C1  玩家 5 分钟内知道怎么玩
  参考 8 号地铁：玩家最多 5 分钟，
  就必须明白"发现异常就回头，没发现就往前走"。
  你们的版本：玩家 5 分钟内必须明白：
  · 我的目标是什么（找到什么/避开什么/解锁什么）
  · 我怎么失败（什么情况 game over）
  · 我怎么赢（通关条件是什么）
  这三个问题不需要文字说明，用场景和行为教会玩家。

  CP-C2  第一层可完整探索并触发 1 个关键事件
  海洋馆第一层，玩家能走完，
  能触发至少 1 个有意义的谜题或线索，
  且该线索与后续推进有关联（不是孤立的装饰）。

  CP-C3  线索与地图绑定（至少 3 处）
  把你们的事件图和实际场景地图对应起来：
  · 脚印出现在哪个房间，和什么剧情有关？
  · 海豚头在什么条件下会改变状态？
  · 哪里是玩家第一个安全区，哪里开始出现威胁？
  把这个对应关系整理成一张表，提交到 GitHub。

  CP-C4  随机事件机制设计（加分项，清明假期任务）
  参考 8 号地铁的"随机灵异事件"：
  不是每次进入同一个房间都发生同样的事。
  至少设计 3 个"小概率触发"的异常事件，
  让玩家每次探索都有意外感。
  用 Claude 帮你梳理哪些场景放什么概率事件最合适
  （把场景截图丢给 Claude 让它建议）。

▌ 中期目标（五一前）
  游戏时长从现在的状态扩充到 30 分钟以上。
  海洋馆至少 2 层（含隐秘区域），
  至少 3 个相互关联的线索链。

▌ GitHub 提交要求
  分支名：group-c-aquarium
  提交内容：Unity 工程 + 第一层可探索场景 + 线索/地图对应表

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROUP D  3人女生组（红喵梦·剧本杀方向）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前进度（本次课）：
✅ 已转型为剧本杀风格，准备较充分
✅ 有完整的人物设定（阴司判官主角）
✅ 审美在线，界面风格统一
⚠️ 随机性机制尚未实现（真凶固定，玩完即弃）
⚠️ Trigger 系统尚未设计

▌ 下下周一检查点（必须完成）

  CP-D1  第一个案件可完整进行
  玩家进入场景→收集线索→作出判断→结算（成功/失败），
  全流程走通，不崩溃。
  时间限制：12 个时辰（游戏内）倒计时必须存在并生效。

  CP-D2  随机真凶机制（核心机制，必须实现）
  同一个案件场景，每次开局真凶从候选人中随机抽取。
  玩家死亡/失败后重进，真凶已更换，
  线索也随之调整。
  这是这个游戏和其他组最大的差异化——必须做出来。
  问 AI：random culprit assignment with dynamic clue generation Unity

  CP-D3  Trigger 三重条件（至少 1 个关键线索）
  至少 1 个关键线索，需要：
  时间条件（白天/黑夜）AND 空间条件（特定地点）
  AND 道具条件（持有某物）同时满足才触发。
  这是深度的来源，不做就是两分钟找到真凶。

  CP-D4  失败惩罚设计
  抓错人 ≠ 什么都没发生。
  需要有明确的惩罚（功德值-X，或某个线索永久消失，
  或某个 NPC 态度改变），
  否则玩家会穷举——依次抓所有人，必然抓到。

▌ 清明假期任务
  三人各认领 1-2 个 NPC，独立写出：
  · 这个人物的行为习惯（早起/晚睡/常去哪）
  · 他/她知道什么秘密（在什么条件下会透露）
  · 他/她与真凶的关系（随机版：与 A 为真凶时的关系 / 与 B 为真凶时的关系）
  三人写完后汇总，找出哪些条件下会碰撞出有效线索。

▌ GitHub 提交要求
  分支名：group-d-hongmiaomeng
  提交内容：Unity 工程或 Figma 原型 + 第一个案件设计文档

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROUP E  小袁（单人·恐怖牌局）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

当前进度：本次课请病假，下次课汇报。

▌ 下下周一检查点

  CP-E1  一局牌局可完整进行
  玩家进入→牌局开始→分出胜负→结算，
  全流程不崩溃。
  胜利获得奖励（法宝/金钱/线索）明确。
  失败有惩罚（损失法宝/金钱）明确。

  CP-E2  与 B 组整合方案（设计文档即可，不需要实现）
  写一页说明：
  · 你的牌局如何作为 B 组捉妖镇的随机事件出现？
  · 在什么条件下（地点/时间/概率）触发牌局？
  · 牌局奖励的"定颜珠"或其他法宝，
    如何成为 B 组破案的一个 Trigger 道具？
  这个整合不是合并两个工程，
  是在设计上互相引用，各自受益。

▌ GitHub 提交要求
  分支名：group-e-yuanpai
  提交内容：Unity 工程 + 一局牌局场景 + B 组整合设计文档

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  全组共同节点
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  周三前      谭老师建 GitHub 仓库，各组收到邀请
  清明假期    好好玩，顺便完成各组分配的设计/写作任务
  下下周一    各组课上现场演示：Unity 里跑通一关
  五一前      各组第一版游戏完成（可完整游玩，有开始有结局）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  所有组的 AI 使用建议
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Trigger 系统设计 → 用 Claude（把场景描述丢给它，
    让它帮你列出所有可能的 trigger 条件组合，
    再检查有没有死循环）

  随机系统逻辑 → 用 Claude 或 Gemini（描述你的随机需求，
    让它给你 Unity C# 实现方案）

  死循环检验 → 把你的线索流程图（截图或文字）
    丢给 Claude，让它找可能出现死循环的节点

  批量碰撞盒（A 组）→ 用 Gemini，
    描述骨骼结构，让它生成自动绑碰撞盒的脚本

  场景线索对应（C 组）→ 把场景截图丢给 Claude，
    让它建议在哪个位置放什么类型的线索

══════════════════════════════════════════════════════════════════
  检查点文档  v1.0  ·  游戏开发班  ·  2026年3月
  一句话总结：清明玩好，回来能跑。
══════════════════════════════════════════════════════════════════
